<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url Shortner</title>
</head>

<body>
    <h1>Welcome to Url Shortner</h1>

    <h3> enter your url below to shorten </h3>

    <% if (locals.id) { %>
        <p>URL Generated: https://localhost:8001/url/<%= id %>
        </p>
        <% } %>


            <div>
                <% if (locals.urls && locals.urls.length > 0) { %>
                    <table>
                        <thead>
                            <th>Sr no.</th>
                            <th>shortId</th>
                            <th>RedirectTo</th>
                            <th>Number of clicks</th>
                        </thead>
                        <tbody>
                            <% urls.forEach((url, index)=> { %>
                                <tr>                                   
                                    <td> <%= index +1 %></td>
                                    <td><%=url.shortId %></td>
                                    <td><%=url.redirectedUrl %></td>
                                    <td><%=url.visitHistory.length %></td>
                                </tr>

                                <% }) %>
                        </tbody>
                    </table>
                    <% } else { %>
                        <div>
                            <form method="post" action="/url">
                                <label for="Enter your url here"></label>
                                <input type="text" name="url" id="url" placeholder="www.example.com">
                                <button type="submit">Generate</button>
                            </form>
                        </div>
                        <% } %>
            </div>

</body>

</html>